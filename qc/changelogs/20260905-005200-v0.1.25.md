# Release v0.1.25 — 2026-09-05

## Summary
Layout Q7/Q8 + EQ toggle + Power debounce — 0.1.25 (26090503, 23M). All gates green (lint 0e, test 20/20+6, roborazzi 6/6, P3 smoke + 8 fontScale + 2 EQ).

## Artifacts
- APK (playstore): `.build-outputs/app-playstore-debug.apk` (23M, `VERSION_CODE` 26090503, `VERSION_NAME` 0.1.25) + `qc/artifacts/apks/app-playstore-debug.apk`
- APK (fdroid): `.build-outputs/app-fdroid-debug.apk` (23M)
- Reports: `qc/reports/lint/lint-results-debug.html` (0 errors, ~130 warns), `qc/reports/tests/` (20/20 + 6 matrix), `qc/reports/roborazzi/matrix-*.png` (6, re-record for layout)
- Screenshots: `qc/artifacts/screenshots/manual/` 16 total (8 fontScale 15K-181K + 6 display-size 15K + 2 smoke 15K/126K + 2 EQ before 15K/128K + 2 landscape 83K/127K, gitignored)
- PresetManager: `ui/components/PresetManagerCard.kt` `showText` icons-only in `EXPANDED` `Pane3` landscape

## QA Results
| Device | Serial | Android | Result | Notes |
|---|---|---|---|---|
| Redmi Note 8 Pro | `hm5xr8gueiz5x4c6` | 16 (SDK 36, 1080×2340, 352dpi) | PASS | `EQ` `+/-` now `isEqEnabled` toggle `OFF→ON` purple `+` + bars visible at bottom edge after scroll, `on>off>on` debounce 500ms |
| ADVAN TAB A10 | `A1013A5320TH000257` | 14 (SDK 34, 1280×800, 601dp) `w962dp` land | PASS | `EXPANDED` `Pane1 QuickBoost` below `Decibel` (was `Pane2`), `Pane3` `Banner + PresetManager(icons) + Battery` (was `Battery` only), `EQ` `+/-` `48dp` + bars `260dp` `120dp` visible after scroll |

## Changelog excerpt (CHANGELOG.md [0.1.25])
See `CHANGELOG.md` [0.1.25] — Added `isEqEnabled` `false` + `PresetManagerCard` separate + `EQ Enabled` `Switch` + layout Q7/Q8; Changed `Pane1/2/3` moves; Fixed `EQ` `+/-` `isEqEnabled` + `Power` `@Synchronized` debounce.

## QC Summary
`qc/QC_SUMMARY.md` 0 OPEN (Run 2026-09-05 00:52 Layout Q7/Q8 + EQ toggle + Power debounce — all green, `isEqEnabled` `false` default, `showText=false` landscape).

## Known Issues
- `EqualizerComponent` preset manager still inside `EQ` for `COMPACT`/`MEDIUM` (now duplicate with separate `PresetManagerCard` in those vertical modes) — will be removed in next PR to keep single source (minimal duplicate for now, per Q10 minimal).
- `Color(0xFF` remaining `38` bespoke warning/button colors in `MainActivity.kt` + `VolumeBoosterService` `8` — deferred low risk.
- No release blockers.

## Recommendations
- Tag `v0.1.25` (`git tag v0.1.25 && git push origin v0.1.25`), `gh release` with `qc/artifacts/apks/*.apk`.
- Next: Remove original preset manager from `EqualizerComponent` (single source) + `Kover` soft coverage.
